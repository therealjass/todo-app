

const initial = {
  user: { data: {}, error: "", loading: false },
}

const todoReducer = (state = initial, action: any) => {
  switch (action?.type) {
  
    default:
      return initial
  }
}

export default todoReducer